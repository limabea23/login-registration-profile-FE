import "./globals.css";

			export const metadata = {
				title: "login-registration-profile-FE",};
			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
}